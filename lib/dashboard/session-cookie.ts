/**
 * Dashboard session cookie (Edge-safe constants for middleware).
 * Must match the cookie set by POST /api/dashboard/auth.
 */
export const DASHBOARD_COOKIE = "dashboard_session";
export const DASHBOARD_COOKIE_VALUE = "ok";
